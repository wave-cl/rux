[package]
name = "rux-proc"
version = "0.46.0"
edition = "2021"

[features]
fuzzing = []

[dependencies]
rux-sched = { path = "../sched" }
rux-klib = { path = "../klib" }
rux-arch = { path = "../arch" }
rux-mm = { path = "../mm" }
