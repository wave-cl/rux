[package]
name = "rux-proc"
version = "0.69.3"
edition = "2021"

[features]
fuzzing = []

[dependencies]
rux-sched = { path = "../sched" }
rux-klib = { path = "../klib" }
rux-arch = { path = "../arch" }
rux-mm = { path = "../mm" }
