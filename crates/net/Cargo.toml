[package]
name = "rux-net"
version = "0.26.0"
edition = "2021"

[dependencies]
smoltcp = { version = "0.13", default-features = false, features = [
    "medium-ethernet", "proto-ipv4",
    "socket-tcp", "socket-udp", "socket-icmp",
] }
